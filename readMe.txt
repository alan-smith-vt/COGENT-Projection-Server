First, install python 3.6.8.

Then, install pre-requisite packages with the following command in a "cmd" terminal. 
Open the terminal within the "localizationServer" folder by clicking where it says "This PC > Local Disk > ect", and typing "cmd" and hitting enter.
(Alternatively hit the windows key, type "cmd", hit enter, and manually navigate to the proper folder using "cd folderName" or "cd "fullPath".

python -m pip install -r requirements.txt

The "python" portion may be "python3", you may also be able to skip the "python -m" and just type starting at "pip"